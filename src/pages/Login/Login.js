import React from 'react';
import {SignIn} from 'components/auth';

/**
 * Firebase Auth UI Sign In authenticator.
 * @return {HTMLElement}
 */
const Login = () => {
  return (
    <>
      <SignIn />
    </>
  );
};

export default Login;
